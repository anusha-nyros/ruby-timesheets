<ul class="nav nav-tabs">
  <li class="<%= action_active('show') %>">
    <%= link_to time_record_path(@pay_period) do %>
        <i class="icon-list-alt"></i>
        Index
    <% end %>
  </li>
  <% if @pay_period && @pay_period.active? %>
  <li class="<%= action_active('new') %>">
    <%= link_to new_time_record_path(pay_period: @pay_period) do %>
      <i class="icon-plus"></i>
      Create New TimeRecord
    <% end %>
  </li>
  <% end %>
</ul>